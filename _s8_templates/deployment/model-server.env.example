# S8 TEMPLATE — never commit real secrets.
APP_PROFILE=model_api
MODEL_RUNTIME=local
INFERENCE_DEVICE=cuda:0
WORKERS=1
HOST=0.0.0.0
PORT=8000
# REMOTE_MODEL_TOKEN=replace-via-approved-secret-management
